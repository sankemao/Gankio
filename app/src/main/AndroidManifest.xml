<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="sankemao.gankio">
    // INTERNET
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.SplashActivity">
        </activity>
        <activity android:name=".ui.activity.mvptest.MvpTestActivity">
        </activity>

        <service android:name="sankemao.baselib.appupdate.service.DownloadService"/>

        <activity android:name=".ui.activity.ImageDetailActivity">
        </activity>
        <activity android:name=".ui.activity.ImageSingleActivity">
        </activity>
        <activity android:name=".ui.activity.test.RecyclerViewTestActivity">
        </activity>
    </application>

</manifest>